import React from 'react';
import { GoogleMap, useJsApiLoader } from '@react-google-maps/api';

const Map = () => {
    return (
        <div>
            <h1>Hello</h1>
        </div>
    );
};

export default Map;